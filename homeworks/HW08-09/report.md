# HW08-09 – PyTorch MLP: регуляризация и оптимизация обучения

## 1. Кратко: что сделано

Выбран датасет CIFAR10 (вариант C), так как он доступен в torchvision и подходит для демонстрации переобучения MLP.

В части A сравнивались:
- базовая модель (E1)
- модель с Dropout (E2)
- модель с BatchNorm (E3)
- модель с EarlyStopping (E4)

В части B сравнивались:
- слишком большой learning rate (O1)
- слишком маленький learning rate (O2)
- SGD + momentum + weight decay (O3)

## 2. Среда и воспроизводимость

- Python: 3.x
- torch / torchvision: PyTorch + torchvision
- Устройство (CPU/GPU): CPU
- Seed: 42
- Как запустить: открыть HW08-09.ipynb и выполнить Run All

## 3. Данные

- Датасет: CIFAR10
- Разделение: train/val/test (train split 80/20 + test из torchvision)
- Трансформации (transform): ToTensor()
- Комментарий: CIFAR10 содержит 10 классов цветных изображений размером 32×32. Для MLP это сложная задача, так как отсутствует учёт пространственной структуры изображений.

## 4. Базовая модель и обучение

- Модель MLP: 2 скрытых слоя (512 и 256 нейронов), активация ReLU
- Loss: CrossEntropyLoss
- Базовый Optimizer (для части A): Adam (lr=1e-3)
- Batch size: 128
- Epochs (макс): 10–20
- EarlyStopping: patience=4, метрика val_accuracy

## 5. Часть A (S08): регуляризация (E1-E4)

- E1 (base): 2 скрытых слоя, без Dropout и BatchNorm
- E2 (Dropout): базовая модель + Dropout(p=0.3)
- E3 (BatchNorm): базовая модель + BatchNorm
- E4 (EarlyStopping): модель с BatchNorm и EarlyStopping

## 6. Часть B (S09): LR, оптимизаторы, weight decay (O1-O3)

- O1: LR слишком большой (Adam, lr=1e-1)
- O2: LR слишком маленький (Adam, lr=1e-5)
- O3: SGD+momentum (momentum=0.9) + weight_decay=1e-4 (lr=1e-2)

## 7. Результаты

Ссылки на файлы в репозитории:

- Таблица результатов: ./artifacts/runs.csv
- Лучшая модель: ./artifacts/best_model.pt
- Конфиг лучшей модели: ./artifacts/best_config.json
- Кривые лучшего прогона: ./artifacts/figures/curves_best.png
- Кривые “плохих LR”: ./artifacts/figures/curves_lr_extremes.png

Короткая сводка:

- Лучший эксперимент части A: E4
- Лучшая val_accuracy: 0.5353
- Итоговая test_accuracy: [впиши число из ноутбука]
- Для O1 (слишком большой LR) обучение нестабильно, loss заметно колеблется
- Для O2 (слишком маленький LR) обучение идёт очень медленно
- O3 (SGD + momentum + weight decay) обучается стабильнее, но медленнее, чем Adam

## 8. Анализ

На графиках видно, что базовая модель (E1) склонна к переобучению: качество на обучающей выборке растёт быстрее, чем на валидационной. Это означает, что модель частично запоминает обучающие данные и хуже обобщает на новые примеры.

Добавление Dropout (E2) уменьшает переобучение, поскольку случайное отключение нейронов во время обучения снижает зависимость модели от отдельных признаков. BatchNorm (E3) делает обучение более стабильным и помогает ускорить сходимость.

Использование EarlyStopping в E4 позволяет остановить обучение в момент, когда качество на validation перестаёт улучшаться, и тем самым избежать дальнейшего переобучения.

В эксперименте O1 слишком большой learning rate приводит к нестабильной оптимизации и заметным колебаниям loss. В O2 слишком маленький learning rate делает обновление весов слишком медленным, поэтому обучение почти не продвигается.

Использование SGD с momentum в O3 делает обновления параметров более сглаженными, а weight decay выступает как дополнительная регуляризация. Однако в данном наборе экспериментов Adam показал более быструю сходимость.

## 9. Итоговый вывод

Лучший результат показала модель с регуляризацией и EarlyStopping. Dropout и BatchNorm уменьшают переобучение и делают обучение более стабильным.

В качестве базового решения можно использовать модель с EarlyStopping и Adam. Для дальнейшего улучшения качества стоит использовать сверточные нейронные сети (CNN) и более точную настройку гиперпараметров.

## 10. Приложение (опционально)

Дополнительные эксперименты не проводились.